{
  "name": "indecision",
  "version": "1.0.0",
  "description": "A simple indecision app",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx babel src/app.js --out-file=public/scripts/app.js --presets=env,react --watch",
    "start--playground": "npx babel src/playground/counter-example.js --out-file=public/scripts/app.js --presets=env,react --watch"
  },
  "author": "Justin Chau",
  "license": "ISC",
  "dependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-env": "^1.5.2",
    "babel-preset-react": "^6.24.1"
  }
}
